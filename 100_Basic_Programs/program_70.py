# 70. Please write a program which accepts basic mathematic expression
# from console and print the evaluation result.

x=input()
print(x)
print((eval(x)))

# eval() evaluates the passed string as a Python expression and returns the result.
# For example, eval("1 + 1") interprets and executes the expression "1 + 1" and returns the result (2)